import { SquidDocId } from '../public-types/communication.public-types';
export type DocHash = string;
export type SecurityResponse = {
    docsToHash: Record<SquidDocId, DocHash | undefined>;
    rulesPassed: true;
} | {
    rulesPassed: false;
};
